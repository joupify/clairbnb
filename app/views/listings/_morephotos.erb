<!-- app/views/listings/show_more_photos.turbo_stream.erb -->
<turbo-stream action="append" target="more_photos">
  <template>
    <% if @additional_photos.present? %>
      <div class="row">
        <% @additional_photos.each do |photo| %>
          <div class="col-6 mb-2">
            <%= image_tag photo.image.url, class: 'd-block w-100' if photo.image.present? %>
          </div>
        <% end %>
      </div>
    <% end %>
  </template>
</turbo-stream>
